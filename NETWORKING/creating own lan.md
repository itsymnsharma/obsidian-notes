
ipconfig - will give ur private ip address in windows
ifconfig for mac

a device doesnt have an ip address
it gets ip address when its connected to a network like if i connect to a router, router will assign me an ip address
same way when i connect to hotspot(this will act as router) it will assign me an ip address

lan usually get ip address like 192.168 since these are reserved for lan aka rpivate ip 

if i change network, connect to wifi thorugh router , or nay other phone, an ip adrress is assigned to us

internet or no internt, if connected a ip address is assigned

this ip address are no unique, it will be unique only in that local area network

when we do ifconfig, we will also get the default gateway aka ip address of the phone or router

any request in our laptop to internet, will go through this default gateway(our phone in this case)
if we had default gateway aka ip of a real router, we cn open it in browser and it shows the router portal(tplink) where we can login this portal is served from our router only as it doesnt depend on data being on, incase of mobile it doesnt go anywhere


a router has two ip address - for a lan and for a wan(internet)

every networking interface has a mac address

openwrt file install in router

in lan, usually first three parts of ip address is same, last one differs

router assigns ip to whoever connects to it


when i create a small html project and opens in localhost it opens at 127.0.0.1 since its loopback address , any request coming will be served by the same computer
our computer connected to internet has an ip address too, if we swap 127.0.0.1  with computer ip address, it will still work

![[Pasted image 20260319033955.png]]


![[Pasted image 20260319034017.png]]